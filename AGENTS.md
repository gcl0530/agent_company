# AGENTS.md

## 1. 作用与优先级

- 本文件定义本仓库长期稳定的协作规则；它不承载阶段结论、临时计划或单份材料的摘要。
- 用户当前直接指令优先于本文件。
- 发生冲突时，按以下顺序判断：可复现的代码、配置和原始数据 > 对应项目的正式设计/研究文档 > 本仓库的导航与说明文档 > 临时材料和个人笔记。
- `README.md` 是仓库入口与导航，不替代材料本身的内容和结论。
- 默认先读取 `README.md` 建立仓库地图，再打开与任务相关的分区和源文件；不要把一次性读遍全部材料当作前置条件。

## 2. 仓库定位与边界

- 本仓库是围绕 Agent 公司建设沉淀的方案、研究、工具和参考资料库，不是单一可运行的软件项目。
- 当前主要分区包括面试系统、研发系统、产品设计系统、项目管理系统、工具链、方法论/调研资料、参考资料及临时工作区。
- 文档、演示材料和脚本可共存，但不得把演示稿、截图或生成产物误作运行事实源。
- 本仓库没有统一构建、部署或测试命令。针对某个脚本或子项目的操作，应在确认其依赖、输入和输出后执行。

## 3. 长期工作原则

- 默认做最小、可核对、可回退的改动；未经用户确认，不重组既有资料或大范围改名。
- 先区分任务是在整理已有材料、形成方案，还是修改可执行工具；三类任务的事实来源和验证方式不同。
- 结论应标明依据、适用范围和未确认部分。材料不足时，明确写出假设，不用推测补成事实。
- 涉及机器人、自动驾驶、ROS2、控制、规划或安全的方案，应优先说明系统边界、输入输出、失效处理和验证口径，不能只给概念性描述。
- 新增内容优先落到职责清晰的现有分区；无法归类的暂存内容放入 `Temp/`，并在正式化前明确归属。
- 不擅自修改、覆盖或删除既有 `docx`、`pdf`、`pptx`、图片等原始材料。需要派生版本时，使用清晰的文件名，并保留原件。
- 不在文档中编造调研数据、外部引用、测试结果或工程状态。

## 4. 材料与事实源规则

### 4.1 可执行资产

- 对 Python、HTML、JSON 等可执行或可解析资产，代码和配置为事实源；说明文档应与其保持一致。
- 修改脚本前，先确认它的输入、输出、依赖和运行入口；修改后至少执行语法/结构检查，条件允许时运行该脚本的最小闭环。
- 图表、图片和报告成品是输出物，不是输入数据的替代品。修改可视化时，应同时核对生成脚本和数据文件。

### 4.2 文档资产

- Markdown、Word、PDF、PPTX 和 HTML 文档都可能承载正式内容；以任务指定版本为准，不根据文件后缀臆断权威性。
- 修改或新建正式方案时，保持“目标/问题 -> 约束与事实 -> 方案与取舍 -> 执行或验证”的论证链，避免只堆砌术语或模板段落。
- 对引用外部资料的结论，保留可追溯来源；无法验证的来源不得被表述为已证实事实。
- `ref_docs/` 中的通用学习与参考资料可用于补充背景，不替代本仓库业务分区中的项目事实、需求或决策结论。
- `Temp/` 和同类临时目录中的内容默认不作为对外或决策依据，除非用户明确指定。

## 5. 协作与表达

- 默认使用中文撰写仓库说明、方案、评审结论和交付说明；必要的技术名词、命令和原文引用可保留英文。
- 在讨论、评审和方案选择中，优先按“问题 - 依据 - 备选项 - 建议 - 风险/验证”表达。
- 对需要用户决策的范围、目标用户、验收标准或发布边界，不擅自替用户决定；先列出影响并请求确认。
- 面向不同对象的材料应保持边界：招聘材料服务于候选人评估，研发材料服务于技术决策和实现，工具链材料服务于复用和操作。不要混合为一份无明确受众的文档。

## 6. 变更与交付

- 修改导航、目录命名或核心材料的定位时，同步检查 `README.md` 是否仍能准确导航到实际内容。
- 声明完成前，至少完成与改动直接相关的验证：文本链接和路径检查、脚本语法或运行检查、文档格式/渲染检查，按风险选择其一或组合。
- 无法运行的验证要说明原因、未覆盖的范围和残余风险。
- 提交前检查当前分支、工作区状态和改动范围，避免混入无关材料或生成物。
